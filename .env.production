# URL da API em produção
# IMPORTANTE: Use HTTPS se o backend tiver SSL configurado
# Se o backend estiver rodando em HTTP, você precisará:
# 1. Configurar HTTPS no backend (recomendado)
# 2. Ou usar um proxy reverso como Nginx com SSL

# Opção 1: Backend com HTTPS (recomendado)
# VITE_API_BASE_URL=https://193.203.183.147:3000

# Opção 2: Backend HTTP (apenas para desenvolvimento, pode causar erro de Mixed Content)
VITE_API_BASE_URL=http://193.203.183.147:3000

# Opção 3: Se usar domínio com SSL
# VITE_API_BASE_URL=https://api.conectaunifepa.com.br
